import { filter, map, tap } from 'rxjs/operators';
import { ModelDynComponent } from './dyn-component.model';
import { BehaviorSubject, Observable, forkJoin } from "rxjs";

export default {
    BehaviorSubject,
    Observable,
    ModelDynComponent,
    filter,
    map,
    tap,
    forkJoin
};